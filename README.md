# N-tier rest API

## Layers

- API
- DataAccess
- Services

Each layer has own project file and presents proper classes. 

## Features
- Project is created using .NET 8 version. 
- To run the project execute `dotnet run` or `dotnet watch run` command in the forceget.API directory. 